<?php
namespace shopack\base\frontend\widgets;

use Yii;
use kartik\form\ActiveField as KartikActiveField;

class ActiveField extends KartikActiveField
{
	/*public function geoMap($options = [])
	{
		// $options = array_replace_recursive([
			// 'style' => 'width:100%; height:300px; border:1px solid black;',
		// ], $options);

		list ($class, $config) = Yii::$app->shopack->widget->defaultWidget(
			\shopack\app\components\shopack\Widget::WIG_GEOMAP);

		return $this->widget($class, array_replace_recursive($config, $options));
	}*/

}
